# Backend
fastapi
uvicorn[standard]
sqlalchemy
alembic
python-dotenv
httpx
pydantic
pydantic-settings

# Database (Postgres for Railway, SQLite for local)
psycopg2-binary

# AI
anthropic

# Scheduling
apscheduler

# Auth — Clerk-signed JWT verification (RS256 via JWKS).
# pyjwt's PyJWKClient handles the JWKS fetch + caching for us.
pyjwt[crypto]
